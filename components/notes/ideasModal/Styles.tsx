import { ViewStyle, TextStyle } from 'react-native';

export interface IdeaStyles {
  card: ViewStyle;
  cardContent: ViewStyle;
  cardText: TextStyle;
  cardDelete: TextStyle;
}

export interface ModalStyles {
    container: ViewStyle;
    close: ViewStyle;
    header: ViewStyle;
    headerText: TextStyle;
    list:ViewStyle;
}